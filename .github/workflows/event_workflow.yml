name: event workflow
on:
  pull_request:
    types:
      - opened
  workflow_dispatch:
  push:
    branches:
      - master
      - 'dev-*'
    paths-ignore:
      - '.github/workflows/*'
jobs:
  test-deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: event data
        run: echo "${{ toJSON(github.event) }}"
      # - name: Get Project Code
      #   uses: actions/checkout@v3
      # - name: Install Deps
      #   run: npm ci
      # - name: Run Tests
      #   run: npm run test
      # - name: build step
      #   run: npm run build
      # - name: deploy step
      #   run: echo "test deploy step"